<%= render 'admin/layouts/application' %>
</div>
</div>
</div>
<div class="innerpage_banner">
<%= image_tag("create.png" , :alt => 'create.png') %>
<div class="container">
<div class="row">
<div class="banner-innerpage">
	<h2>Be<span>St</span>aff</h2>
</div>
</div>
</div>
</div><!-- end .innerpage_banner -->
</div><!-- end. header -->
<div class="content inner-page">
	<div class="content-down"></div>
<div class="container">
    <% flash.each do |name, msg| %>
      <div class="">
        <h4 align="center" class="flash"><%= content_tag :div, msg, :id => "flash_#{name}" %>
        </h4>
      </div>
    <% end %>
<div class="row">
	<h4><span>
		<%= image_tag("line.png" , :alt => 'line.png') %>
		</span>Qualification Page<span><%= image_tag("right.png" , :alt => 'right.png') %></span></h4>
		<span class="q1">Admin will see list and will add new qualification from here
		</span>
		<a class="add-new" href="<%=  new_admin_qualification_path %>" > Add Qalification</a>
		<div class="table-responsive">

			<%#= debug @all_skills  %>
			<table class="table">
				<thead>
					<tr>
						<th>No</th>
						<th>Qualification</th>
						<th>Actions</th>
					</tr>
				</thead>
				<tbody>
					<% @all_qualification.each do |qualification| %>
					<tr>
						<td><%= qualification.id %></td>
						<td><%= qualification.qualification %></td>
						<td><a class="edit" href="/admin/qualifications/<%= qualification.id  %>/edit"></a> <a class="delete" href="/admin/qualifications/<%= qualification.id  %>/delete"></a></td>						
					</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>
</div>
<%= render 'admin/layouts/footer' %>
